{
  "name": "git-desktop-browser--main-process",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "rimraf build && tsc",
    "lint": "eslint src --max-warnings=0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
