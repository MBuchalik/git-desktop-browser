{
  "name": "git-desktop-browser--main-process",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean": "rimraf build/",
    "build": "npm run clean && tsc",
    "lint": "eslint src --max-warnings=0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
